import CreateWorldForm from './CreateWorldForm';

const Home = () => {
  return (
    <main className="flex min-h-screen bg-slate-300">
      <CreateWorldForm />
    </main>
  );
};

export default Home;
